---
type: defect
title: 'BUG-068 — `lcode.zig`: Underflow panic in `ceillog2(0)`'
description: 'ceillog2(x: u32) executes var v = x; v -= 1;.'
tags:
- bugs
- defect
- med
- fixed
- compiler
timestamp: '2026-08-08T00:00:00Z'
---

# BUG-068 — `lcode.zig`: Underflow panic in `ceillog2(0)`

- **Severity:** [MED]
- **Status:** ✅ FIXED

- **Location:** `src/lcode.zig:1264`.
- **Defect:** `ceillog2(x: u32)` executes `var v = x; v -= 1;`.
- **Impact:** `ceillog2(0)` triggers an unsigned integer underflow panic in Debug mode.
- **Fix:** Add guard `if (x == 0) return 0;`.

---
[← Back to Bug Index](index.md)
