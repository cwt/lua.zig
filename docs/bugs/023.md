---
type: defect
title: BUG-023 — `openio` leaks the FILE* metatable on the stack
description: luaL_newmetatable + luaL_setfuncs(L, &flib, 0) left the FILE
tags:
- bugs
- defect
- low
- fixed
- io
timestamp: '2026-07-14T16:10:00Z'
---

# BUG-023 — `openio` leaks the FILE* metatable on the stack

- **Severity:** [LOW]
- **Status:** ✅ FIXED

- **Location:** `src/lib/iolib.zig:386-390` (`openio`).
- **Defect:** `luaL_newmetatable` + `luaL_setfuncs(L, &flib, 0)` left the FILE*
  metatable on the stack; `lua_setglobal(L, "io")` only pops the io table.
- **Fix:** Added `lua.lua_pop(L_, 1)` after `luaL_setfuncs(L_, &flib, 0)`.

---
[← Back to Bug Index](index.md)
