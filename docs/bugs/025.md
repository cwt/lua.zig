---
type: defect
title: BUG-025 — Empty `catch {}` in `luaL_setfuncs` / `luaL_fileresult`
description: lua_setfield call wrapped in catch {}, silently discarding errors.
tags:
- bugs
- defect
- low
- fixed
- auxlib
timestamp: '2026-07-14T16:10:00Z'
---

# BUG-025 — Empty `catch {}` in `luaL_setfuncs` / `luaL_fileresult`

- **Severity:** [LOW]
- **Status:** ✅ FIXED

- **Location:** `src/lauxlib.zig:364-370` (`luaL_setfuncs`).
- **Defect:** `lua_setfield` call wrapped in `catch {}`, silently discarding errors.
- **Fix:** Changed `luaL_setfuncs` and `luaL_newlib` to `!void` and use `try`.
  Updated all callers.

---
[← Back to Bug Index](index.md)
