---
type: defect
title: 'BUG-112 — `lib/iolib.zig`: Sentinel slice evaluated before NUL byte initialization
  in `io_tmpfile`'
description: buf[0..path.len :0].ptr is evaluated before line 226 sets buf[path.len]
  = 0.
tags:
- bugs
- defect
- med
- fixed
- io
timestamp: '2026-08-08T00:00:00Z'
---

# BUG-112 — `lib/iolib.zig`: Sentinel slice evaluated before NUL byte initialization in `io_tmpfile`

- **Severity:** [MED]
- **Status:** ✅ FIXED

- **Location:** `src/lib/iolib.zig:219`.
- **Defect:** `buf[0..path.len :0].ptr` is evaluated before line 226 sets `buf[path.len] = 0`.
- **Impact:** In Debug and ReleaseSafe modes, creating a sentinel slice `[:0]` over uninitialized stack memory triggers a runtime panic.
- **Fix:** Set `buf[path.len] = 0` before calling `openatZ` with `[:0]` sentinel slice in `io_tmpfile`. **2026-08-08**.

---
[← Back to Bug Index](index.md)
