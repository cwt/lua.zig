---
type: defect
title: 'BUG-081 — `lib/iolib.zig`: Raw `std.c` POSIX syscalls bypass `std.Io` capability
  interface'
description: File stream operations call std.c.open, std.c.close, std.c.unlink, std.c.write
  directly.
tags:
- bugs
- defect
- low
- fixed
- io
timestamp: '2026-08-08T00:00:00Z'
---

# BUG-081 — `lib/iolib.zig`: Raw `std.c` POSIX syscalls bypass `std.Io` capability interface

- **Severity:** [LOW]
- **Status:** ✅ FIXED

- **Location:** `src/lib/iolib.zig:51`, `74`, `76`, `86`, `162`, `194`, `221`.
- **Defect:** File stream operations call `std.c.open`, `std.c.close`, `std.c.unlink`, `std.c.write` directly.
- **Impact:** Violates §0.1 Rule 10 by bypassing the explicit `std.Io` capabilities initialized in `global_State`.
- **Fix:** Refactor file streams to use `std.Io` capabilities.

---
[← Back to Bug Index](index.md)
