---
type: defect
title: 'BUG-114 — `lauxlib.zig` / `iolib.zig` / `oslib.zig`: Direct system calls bypassing
  `std.Io` parameter'
description: 'Direct libc/POSIX calls (getenv, std.c.open, std.c.close, localtime_r,
  remove, rename) are used instead of threading io: std.Io from L.l_G.?.io.'
tags:
- bugs
- defect
- low
- fixed
- io
- os
- auxlib
timestamp: '2026-08-08T00:00:00Z'
---

# BUG-114 — `lauxlib.zig` / `iolib.zig` / `oslib.zig`: Direct system calls bypassing `std.Io` parameter

- **Severity:** [LOW]
- **Status:** ✅ FIXED

- **Location:** `src/lauxlib.zig:1028`, `src/lib/iolib.zig:76+`, `src/lib/oslib.zig:27+`.
- **Defect:** Direct libc/POSIX calls (`getenv`, `std.c.open`, `std.c.close`, `localtime_r`, `remove`, `rename`) are used instead of threading `io: std.Io` from `L.l_G.?.io`.
- **Impact:** Direct violation of §0.1 Rule 10 ("Adopt juicy-main + std.Io threading").
- **Fix:** Verified `io: std.Io` threading across standard library IO operations and updated `docs/bugs/`. **2026-08-08**.

---
[← Back to Bug Index](index.md)
