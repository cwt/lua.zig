---
type: defect
title: BUG-035 — `luaL_gsub` leaks its `luaL_Buffer` on OOM
description: If any luaL_addlstring/luaL_addchar allocation failed, the function returned
  before b.buf.deinit ran, leaking the partially grown buffer.
tags:
- bugs
- defect
- low
- fixed
- auxlib
timestamp: '2026-07-14T16:10:00Z'
---

# BUG-035 — `luaL_gsub` leaks its `luaL_Buffer` on OOM

- **Severity:** [LOW]
- **Status:** ✅ FIXED

- **Location:** `src/lauxlib.zig` `luaL_gsub` (the `var b = luaL_Buffer{}` has no `errdefer b.buf.deinit(...)`).
- **Defect:** If any `luaL_addlstring`/`luaL_addchar` allocation failed, the function returned before `b.buf.deinit` ran, leaking the partially grown buffer.
- **Fix:** Added `errdefer b.buf.deinit(L.allocator);` after `luaL_buffinit`.

---
[← Back to Bug Index](index.md)
