---
type: defect
title: BUG-024 — Hardcoded `std.heap.page_allocator` in iolib read/line helpers
description: Buffers allocated via std.heap.page_allocator instead of threading
tags:
- bugs
- defect
- low
- fixed
- io
timestamp: '2026-07-14T16:10:00Z'
---

# BUG-024 — Hardcoded `std.heap.page_allocator` in iolib read/line helpers

- **Severity:** [LOW]
- **Status:** ✅ FIXED

- **Location:** `src/lib/iolib.zig:164-165` (`read_chars`), `:172-187` (`read_line`),
  `:332-348` (`f_lines`).
- **Defect:** Buffers allocated via `std.heap.page_allocator` instead of threading
  the state allocator.
- **Fix:** Changed to `L_.allocator` (threaded through the function parameters).

---
[← Back to Bug Index](index.md)
