---
type: defect
title: 'BUG-074 — `lparser.zig`: Double free and slice leak in `close_func`'
description: If any toOwnedSlice call fails midway, errdefer calls .deinit() on empty
  ArrayLists (leaking previously converted slices) and body()'s errdefer calls...
tags:
- bugs
- defect
- med
- fixed
- compiler
timestamp: '2026-08-08T00:00:00Z'
---

# BUG-074 — `lparser.zig`: Double free and slice leak in `close_func`

- **Severity:** [MED]
- **Status:** ✅ FIXED

- **Location:** `src/lparser.zig:880-896`, `909` (`close_func`).
- **Defect:** If any `toOwnedSlice` call fails midway, `errdefer` calls `.deinit()` on empty `ArrayList`s (leaking previously converted slices) and `body()`'s `errdefer` calls `.deinit()` a second time.
- **Impact:** Double free panic and memory leaks during parse errors.
- **Fix:** Restructure `close_func` slice conversion cleanup using explicit optional slice tracking.

---
[← Back to Bug Index](index.md)
