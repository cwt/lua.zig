---
type: defect
title: 'BUG-069 — `ltable.zig`: Unchecked negative index cast panic in hash chain
  search'
description: while (t.node.items[prev].next != -1 and @as(usize, @intCast(t.node.items[prev].next))
  != mp) casts negative .next values.
tags:
- bugs
- defect
- high
- fixed
- table
timestamp: '2026-08-08T00:00:00Z'
---

# BUG-069 — `ltable.zig`: Unchecked negative index cast panic in hash chain search

- **Severity:** [HIGH]
- **Status:** ✅ FIXED

- **Location:** `src/ltable.zig:359-364` (`setHash`).
- **Defect:** `while (t.node.items[prev].next != -1 and @as(usize, @intCast(t.node.items[prev].next)) != mp)` casts negative `.next` values.
- **Impact:** If `.next` contains any negative value other than `-1` (corrupted flag or invalid index), `@intCast` to `usize` panics.
- **Fix:** Check `t.node.items[prev].next >= 0` before casting to `usize`.

---
[← Back to Bug Index](index.md)
