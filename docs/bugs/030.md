---
type: defect
title: BUG-030 — Memory leak of dynamic `path` in `searchpath`
description: path allocated via std.mem.replaceOwned was leaked when returning from
  searchpath.
tags:
- bugs
- defect
- low
- fixed
- loadlib
- cli
- gc
timestamp: '2026-07-14T16:10:00Z'
---

# BUG-030 — Memory leak of dynamic `path` in `searchpath`

- **Severity:** [LOW]
- **Status:** ✅ FIXED

- **Location:** `src/lib/loadlib.zig:159-183` (`searchpath`).
- **Defect:** `path` allocated via `std.mem.replaceOwned` was leaked when returning from `searchpath`.
- **Fix:** Added `defer L.allocator.free(path)` and returned a GC-managed copy pushed onto the stack via `lua.lua_tostring(L, -1).?`.

---
[← Back to Bug Index](index.md)
