---
type: defect
title: 'BUG-075 — `lua.zig`: Unconditional free of externally-owned strings in state
  teardown'
description: close_state calls g.allocator.free(key) on all string table entries without
  checking ts.externally_owned.
tags:
- bugs
- defect
- med
- fixed
timestamp: '2026-08-08T00:00:00Z'
---

# BUG-075 — `lua.zig`: Unconditional free of externally-owned strings in state teardown

- **Severity:** [MED]
- **Status:** ✅ FIXED

- **Location:** `src/lua.zig:5821-5825` (`close_state`).
- **Defect:** `close_state` calls `g.allocator.free(key)` on all string table entries without checking `ts.externally_owned`.
- **Impact:** Invalid free crash when closing state with external or static strings in the string table.
- **Fix:** Add `if (!ts.externally_owned) g.allocator.free(key);` check in `close_state`.

---
[← Back to Bug Index](index.md)
