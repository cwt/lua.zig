---
type: defect
title: 'BUG-098 — `lib/mathlib.zig`: Unsigned integer cast overflow panic in `math.random`'
description: Loop while ((lim & (lim +% 1)) != 0) doubles sh (sh = 2).
tags:
- bugs
- defect
- med
- fixed
- math
timestamp: '2026-08-08T00:00:00Z'
---

# BUG-098 — `lib/mathlib.zig`: Unsigned integer cast overflow panic in `math.random`

- **Severity:** [MED]
- **Status:** ✅ FIXED

- **Location:** `src/lib/mathlib.zig:286-290` (`project`).
- **Defect:** Loop `while ((lim & (lim +% 1)) != 0)` doubles `sh` (`sh *= 2`). `@as(u6, @intCast(sh))` panics when `sh == 64`.
- **Impact:** Process panic in `math.random` when `sh` reaches 64.
- **Fix:** Added a guard `if (sh >= 64) break;` inside the loop to cap `sh` before it overflows the `u6` cast. **2026-08-08**.

---
[← Back to Bug Index](index.md)
