---
type: defect
title: 'BUG-095 — `lparser.zig`: Premature state mutation in `newupvalue` on missing
  enclosing function'
description: allocupvalue(fs) mutates fs.upvalues and fs.nups before validating fs.prev
  orelse return llex.luaX_syntaxerror(...).
tags:
- bugs
- defect
- med
- fixed
- compiler
timestamp: '2026-08-08T00:00:00Z'
---

# BUG-095 — `lparser.zig`: Premature state mutation in `newupvalue` on missing enclosing function

- **Severity:** [MED]
- **Status:** ✅ FIXED

- **Location:** `src/lparser.zig:455-467` (`newupvalue`).
- **Defect:** `allocupvalue(fs)` mutates `fs.upvalues` and `fs.nups` *before* validating `fs.prev orelse return llex.luaX_syntaxerror(...)`.
- **Impact:** If `fs.prev` is null, `fs` is left in a corrupted state with a half-initialized upvalue.
- **Fix:** Validate `fs.prev != null` before calling `allocupvalue(fs)`. **2026-08-08**.

---
[← Back to Bug Index](index.md)
