---
type: defect
title: 'BUG-070 — `ldump.zig`: Debug info stripping format protocol discrepancy'
description: When strip == true, ldump.zig emits f.locvars.len and f.upvalues.len
  filled with empty strings instead of 0.
tags:
- bugs
- defect
- low
- fixed
- bytecode
timestamp: '2026-08-08T00:00:00Z'
---

# BUG-070 — `ldump.zig`: Debug info stripping format protocol discrepancy

- **Severity:** [LOW]
- **Status:** ✅ FIXED

- **Location:** `src/ldump.zig:187`, `195`.
- **Defect:** When `strip == true`, `ldump.zig` emits `f.locvars.len` and `f.upvalues.len` filled with empty strings instead of 0.
- **Impact:** Stripped bytecode chunks produced by `lua_dump` contain redundant zero-length string entries, violating the reference Lua binary format.
- **Fix:** Emit count `0` when `strip` is `true`.

---
[← Back to Bug Index](index.md)
