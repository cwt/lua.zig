---
type: defect
title: BUG-022 — `os.remove` ignores the syscall result and always returns `true`
description: linux.unlink return value was discarded; always pushed true.
tags:
- bugs
- defect
- med
- fixed
- os
timestamp: '2026-07-14T16:10:00Z'
---

# BUG-022 — `os.remove` ignores the syscall result and always returns `true`

- **Severity:** [MED]
- **Status:** ✅ FIXED

- **Location:** `src/lib/oslib.zig:18-25` (`os_remove`).
- **Defect:** `linux.unlink` return value was discarded; always pushed `true`.
- **Fix:** Check `linux.unlink` result, push `false` on failure instead of `true`.

---
[← Back to Bug Index](index.md)
